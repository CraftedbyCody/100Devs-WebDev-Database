FEATURE TO ADD LATER:

create a button on nav bar that that loads a random post from the database

at first, only show the anki question section of the post

create a button that shows the rest of the post (image, caption, source link, etc) after the user has clicked the button 